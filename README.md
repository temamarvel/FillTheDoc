# FillTheDoc

`FillTheDoc` — нативное macOS-приложение для полуавтоматического заполнения `DOCX`-шаблонов на основе реквизитов компании.

Приложение помогает пройти весь сценарий в одном окне:
- загрузить шаблон `DOCX`;
- загрузить файл с реквизитами компании (`DOC`, `DOCX`, `PDF`, `XLS`, `XLSX`, `TXT`);
- извлечь текст из файла локально на Mac;
- распознать реквизиты через LLM;
- проверить и при необходимости исправить поля вручную;
- заполнить шаблон и сохранить новый `DOCX`;
- дополнительно получить TSV-строку для вставки в Google Sheets.

## Для чего это нужно

`FillTheDoc` полезен, если вы регулярно:
- заполняете типовые договоры, анкеты и шаблоны;
- получаете реквизиты клиентов в разных форматах;
- хотите сократить ручной copy/paste;
- хотите оставить человеку финальную проверку перед выгрузкой документа.

## Что умеет приложение

- Работает как нативное приложение для macOS на `SwiftUI`.
- Принимает шаблон только в формате `DOCX`.
- Извлекает текст из файлов реквизитов в форматах:
  - `DOC`
  - `DOCX`
  - `PDF`
  - `XLS`
  - `XLSX`
  - `TXT`
- Использует OpenAI-модель `gpt-4o-mini` для преобразования извлечённого текста в structured draft.
- Даёт пользователю форму подтверждения и редактирования данных перед заполнением шаблона.
- Валидирует поля локально и частично сверяет их с DaData.
- Поддерживает заполнение плейсхолдеров в `DOCX`, включая основные XML-части документа.
- Экспортирует готовый документ через стандартное окно сохранения macOS.
- Формирует TSV-строку для вставки в Google Sheets и копирует её в буфер обмена.
- Поддерживает встроенные и пользовательские плейсхолдеры через единый runtime registry.

## Требования к окружению

По текущим настройкам проекта:
- целевая платформа — `macOS 15.6+`;
- проект создан и обновлялся в линии `Xcode 26.x`;
- для office-extraction используется системная утилита `textutil`, поэтому сценарий привязан к macOS;
- приложению нужны исходящие сетевые запросы для OpenAI и DaData;
- приложение работает в sandbox и использует доступ к файлам, выбранным пользователем.

## Зависимости проекта

Проект подключает следующие Swift Package зависимости:

| Пакет | Назначение | Минимальная версия |
| --- | --- | --- |
| `DocxUtils` | сканирование, условная сборка и заполнение `DOCX`-шаблонов | `0.0.2` |
| `OpenAIClient` | запрос к OpenAI и декодирование structured output | `0.0.1` |
| `DaDataAPIClient` | справочная сверка реквизитов через DaData | `0.0.1` |
| `SwiftUI-Shimmer` | shimmer-анимация для индикатора ожидания | `1.5.1` |

Системные фреймворки, на которые опирается приложение:
- `SwiftUI`
- `PDFKit`
- `AppKit`
- `UniformTypeIdentifiers`
- `Foundation`

## Конфигурация

### `Secrets.xcconfig`

Проект использует `Secrets.xcconfig` как base configuration для target'а.

Сейчас из него читается как минимум:
- `DADATA_TOKEN` — токен для справочной валидации через DaData.

Рекомендации для локальной разработки:
- держите реальный токен только в локальной конфигурации;
- не публикуйте production-секреты в открытом репозитории;
- если DaData не нужна, закладывайтесь на fail-soft поведение: форма продолжит работать и без успешной внешней сверки.

### OpenAI API key

OpenAI API key не зашит в проект:
- пользователь вводит его при первом запуске автоматического извлечения;
- ключ сохраняется в `Keychain` текущего Mac;
- без него нельзя запустить LLM extraction, но форму всё равно можно заполнить вручную.

## Быстрый старт для разработки

1. Откройте `FillTheDoc.xcodeproj` в Xcode.
2. Дождитесь загрузки Swift Package зависимостей.
3. Проверьте локальную конфигурацию `Secrets.xcconfig`.
4. Запустите target `FillTheDoc`.
5. При первом автоматическом извлечении введите OpenAI API key в модальном окне.

Если вы хотите просто посмотреть UI, начните с `MainView.swift`, `MainViewModel.swift` и preview'ев во view-файлах.

## Как это работает

Базовый сценарий такой:

1. Вы перетаскиваете в приложение шаблон `DOCX`.
2. Вы перетаскиваете файл с реквизитами компании.
3. Приложение извлекает из него текст локально на Mac.
4. Извлечённый текст отправляется в OpenAI для построения черновика extracted placeholder values.
5. Черновик extracted placeholder values передаётся в форму как стартовые значения.
6. Вы проверяете форму, исправляете ошибки и нажимаете **«Применить»**.
7. После подтверждения данных становится доступна кнопка **«Заполнить шаблон»**.
8. Приложение подставляет значения в плейсхолдеры шаблона и предлагает сохранить новый `DOCX`.
9. После успешной подготовки документа дополнительно формируется TSV-строка для Google Sheets.

## Архитектура проекта

Ниже — не просто список папок, а карта ответственности слоёв.

### 1. Точка входа и orchestration UI

- `FillTheDoc/FillTheDocApp.swift` — точка входа приложения.
- `FillTheDoc/Views/MainView/MainView.swift` — корневой экран.
- `FillTheDoc/Views/MainView/MainViewModel.swift` — главный координатор пользовательского сценария.

`MainViewModel` — центральный orchestration-слой приложения. Он:
- принимает события от UI;
- вызывает специализированные сервисы;
- хранит observable state;
- следит, чтобы шаги сценария происходили в правильном порядке.

При этом он намеренно не знает деталей:
- как извлекается текст из конкретного формата;
- как устроен prompt к модели;
- как валидируются отдельные поля;
- как заменяются XML-токены внутри `DOCX`.

### 2. Извлечение текста из входных файлов

Папки:
- `FillTheDoc/TextExtraction/`
- `FillTheDoc/Extractors/`

Здесь есть два уровня:
1. `DocumentTextExtractorService` — верхнеуровневый фасад extraction-пайплайна.
2. Конкретные адаптеры (`PlainTextExtractor`, `PDFKitTextExtractor`, `TextutilOfficeExtractor`) — низкоуровневые механизмы для отдельных форматов.

Почему сделано именно так:
- UI и orchestration не должны знать, чем отличается `PDFKit` от `textutil`;
- конкретные extractors проще тестировать и заменять по отдельности;
- фасад централизует sandbox access, временные копии, нормализацию текста и сбор диагностики.

### 3. Граница между LLM и внутренней моделью

Папки:
- `FillTheDoc/DataModels/`
- `FillTheDoc/Placeholder/ExtractedPlaceholderValues.swift`

Ключевые типы:
- `ExtractedPlaceholderValues` — typed-обёртка над ответом LLM для extracted placeholder-значений;
- `LegalForm` — типизированный справочник правовых форм и их нормализация.

Это важная граница проекта:
- LLM возвращает черновик extracted placeholder values;
- этот словарь напрямую становится стартовым состоянием формы;
- пользователь затем подтверждает или исправляет этот черновик;
- только после подтверждения placeholder-map считается источником истины для заполнения шаблона.

### 4. Placeholder-domain

Папка: `FillTheDoc/Placeholder/`

Это слой, который объясняет приложению:
- какие ключи вообще существуют;
- как их показывать в UI;
- как их валидировать;
- как вычислять итоговые строки для шаблона.

Ключевые типы:
- `PlaceholderKey` — identity плейсхолдера;
- `PlaceholderDescriptor` — метаданные для UI и справочника плейсхолдеров;
- `PlaceholderBehavior` — runtime-policy для normalizer/validator/resolver;
- `PlaceholderRegistry` — единый реестр знаний о плейсхолдерах;
- `PlaceholderResolutionContext` — контекст, из которого вычисляются значения;
- `TemplatePlaceholderResolver` — сборщик итогового словаря значений;

Почему это отдельный слой, а не логика во view model:
- UI, валидация, библиотека плейсхолдеров и DOCX fill опираются на одни и те же определения;
- так derived-поля, normalizer'ы и список ключей не расходятся по проекту.

### 5. Подтверждение данных пользователем

Папки:
- `FillTheDoc/Views/DocumentDataView/`
- `FillTheDoc/Validation/`

После ответа модели пользователь видит форму подтверждения.

Это отдельный этап по смыслу:
- до нажатия **«Применить»** данные считаются только предварительным черновиком;
- после нажатия **«Применить»** формируется финальный набор значений для шаблона;
- внешняя сверка с DaData даёт подсказки, но не переписывает значения автоматически.

Такой компромисс выбран специально: приложение экономит ручной труд, но оставляет человеку финальное решение в юридически чувствительном сценарии.

### 6. Заполнение `DOCX`

Основной orchestration живёт в `MainViewModel`, а техническая работа с шаблоном делегируется зависимостям из `DocxUtils` и helper-типам проекта.

Пайплайн такой:
1. из формы получается словарь `[PlaceholderKey: String]`;
2. словарь переводится в string-keyed формат для шаблонизатора;
3. условные блоки шаблона сначала собираются и раскрываются;
4. затем выполняется обычная подстановка значений;
5. результат экспортируется через системный `fileExporter`.

Порядок «сначала условные блоки, потом обычная замена» важен: control tokens не являются обычными пользовательскими плейсхолдерами и должны обрабатываться отдельным этапом.

### 7. Инфраструктурные сервисы

- `Keychain/` — безопасное хранение OpenAI API key.
- `AppUpdate/` — проверка новых версий через GitHub Releases.
- `Views/CopyString/` — формирование TSV-строки для внешнего процесса работы с Google Sheets.
- `Placeholder/CustomPlaceholder*` — persistence и валидация пользовательских плейсхолдеров.

Это вспомогательные подсистемы. Они не участвуют напрямую в расчёте core-данных компании, но закрывают реальные рабочие сценарии приложения.

## Как читать код с нуля

Если вы впервые открыли проект, самый быстрый маршрут такой:

1. `README.md` — общий сценарий и ограничения.
2. `FillTheDocApp.swift` — точка входа.
3. `MainView.swift` — как выглядит корневой экран.
4. `MainViewModel.swift` — основной пользовательский flow.
5. `DocumentTextExtractorService.swift` — как любой входной файл превращается в текст.
6. `ExtractedPlaceholderValues.swift`, `LegalForm.swift` и `PromptBuilder.swift` — контракт между приложением и LLM.
7. Папка `Placeholder/` — как устроена доменная модель плейсхолдеров.
8. `DocumentDataFormView.swift`, `DocumentDataFormViewModel.swift` и `DocumentDataFieldView.swift` — где черновик превращается в подтверждённые данные формы.
9. `CompanyReferenceValidator.swift` — как работает внешняя справочная сверка.

## Ключевые архитектурные компромиссы

- **Human-in-the-loop вместо полного автомата.** LLM ускоряет извлечение, но не заменяет ручное подтверждение.
- **macOS-first extraction.** Для office-файлов используется системный `textutil`, потому что это упрощает стек зависимостей, но делает поведение привязанным к macOS.
- **Soft-fail в extraction.** Если текст извлечь не удалось, pipeline старается вернуть диагностируемый пустой результат, а не мгновенно ронять весь сценарий.
- **Единый placeholder-registry.** Это немного увеличивает число типов, но резко снижает риск рассинхронизации между UI, валидацией и export.
- **После `Применить` форма становится источником истины.** Не исходный JSON модели, а подтверждённые пользователем значения идут в шаблон и в строку для Google Sheets.

## Использование

### Шаг 1. Загрузите шаблон

Левая зона принимает только шаблон `DOCX`:
- заголовок в UI: **«Шаблон (DOCX)»**.

Если шаблон существует и доступен, зона считается валидной.

### Шаг 2. Загрузите реквизиты

Правая зона принимает файл с реквизитами:
- `DOC`
- `DOCX`
- `PDF`
- `XLS`
- `XLSX`
- `TXT`

После загрузки приложение автоматически запускает извлечение текста и распознавание реквизитов.

### Шаг 3. Подтвердите данные

После распознавания появляется форма, где можно:
- проверить поля компании;
- вручную исправить ошибки;
- ввести:
  - `Номер документа`
  - `Комиссия, %`
  - `Мин. комиссия, руб`
  - `Способ оплаты`

Поля валидируются локально. Для части реквизитов дополнительно выполняется сверка с DaData после потери фокуса или по явному действию пользователя.

### Шаг 4. Заполните шаблон

Кнопка **«Заполнить шаблон»** активируется только если:
- шаблон существует;
- файл реквизитов существует;
- есть сохранённый OpenAI API key;
- пользователь нажал **«Применить»** и подтвердил данные формы.

После нажатия приложение:
- создаёт временный заполненный `DOCX`;
- открывает системный экспортёр macOS;
- предлагает имя вида `<template>_filled.docx`;
- формирует строку для Google Sheets и копирует её в буфер обмена.

## Поддерживаемые входные форматы

| Тип файла | Поддержка | Как обрабатывается |
| --- | --- | --- |
| `DOCX` | Да | Через системный office extractor |
| `DOC` | Да | Через системный office extractor |
| `PDF` | Да | Через `PDFKit` |
| `XLS` | Да | Через системный office extractor |
| `XLSX` | Да | Через системный office extractor |
| `TXT` | Да | Как plain text |
| Другие расширения | Нет | Возвращается ошибка неподдерживаемого формата |

### Важные ограничения extraction

- Для `PDF` приложение извлекает только доступный текстовый слой, но не делает OCR.
- Если PDF состоит только из сканов или картинок, результат может быть пустым и помечаться как `needsOCR`.
- Извлечённый текст нормализуется и обрезается до `60_000` символов.
- Для `DOC`, `DOCX`, `XLS`, `XLSX` используется внешний механизм текстового извлечения на базе системных утилит macOS.

## Поддерживаемые поля

### Extracted-поля компании

LLM заполняет extracted placeholder-map следующими полями:

| Ключ | Что означает |
| --- | --- |
| `company_name` | Название компании |
| `legal_form` | Правовая форма (`ООО`, `АО`, `ИП` и т.д.) |
| `ceo_full_name` | ФИО руководителя |
| `ceo_full_genitive_name` | ФИО руководителя в родительном падеже |
| `ceo_shorten_name` | Краткое ФИО руководителя |
| `ogrn` | ОГРН / ОГРНИП |
| `inn` | ИНН |
| `kpp` | КПП |
| `email` | Email |
| `address` | Юридический адрес |
| `phone` | Телефон |

### Ручные поля документа

Форма и placeholder-registry дополнительно содержат ручные поля документа:

| Ключ | Источник |
| --- | --- |
| `document_number` | Вводится пользователем |
| `fee` | Вводится пользователем |
| `min_fee` | Вводится пользователем |
| `payment_method` | Выбирается пользователем из списка |

### Вычисляемые поля

В итоговый словарь для шаблона также добавляются вычисляемые поля:

| Ключ | Источник |
| --- | --- |
| `full_company_name` | Формируется из правовой формы и названия |
| `full_company_name_expanded` | Формируется из полной правовой формы и названия |
| `date` | Текущая дата в формате `«dd» MMMM yyyy г.` |
| `date_short` | Текущая дата в формате `dd.MM.yyyy` |
| `ceo_role` | `Генеральный директор` или `Индивидуальный предприниматель` |
| `rules` | `Устава` или текст про запись в ЕГРИП |

## Плейсхолдеры в шаблоне `DOCX`

Приложение ищет и заменяет плейсхолдеры в формате:

```text
<!company_name!>
<!inn!>
<!full_company_name!>
<!date!>
```

### Пример

Если в шаблоне есть текст:

```text
Исполнитель: <!full_company_name!>, ИНН <!inn!>
```

то после заполнения он будет заменён значениями из формы.

### Что важно учитывать

- Если в шаблоне есть ключ, которого нет в словаре значений, по умолчанию плейсхолдер сохраняется как есть.
- Замена работает по XML-частям `DOCX`, а не по визуальному тексту из Word.
- Сканер и replacer умеют собирать плейсхолдеры даже если текст разбит на несколько runs внутри абзаца.
- Обрабатываются стандартные части документа; поддержка сносок, endnotes и comments включена.
- Условные блоки шаблона используют отдельные control tokens и обрабатываются отдельным этапом.

## Валидация данных

### Локальная валидация

Форма проверяет:
- непустое название компании;
- ИНН;
- КПП;
- ОГРН / ОГРНИП;
- ФИО;
- краткое ФИО;
- email;
- адрес;
- телефон;
- правовую форму;
- значения комиссии.

### Сверка с DaData

После ввода или правки ключевых реквизитов приложение может дополнительно показать предупреждения, если данные слабо совпадают с DaData, например:
- ИНН не совпадает;
- КПП не совпадает;
- ОГРН не совпадает;
- название компании слабо похоже;
- ФИО руководителя слабо похоже;
- адрес слабо похож.

Это именно предупреждения для оператора, а не автоматическое исправление данных.

## Google Sheets

После успешной подготовки документа приложение формирует TSV-строку и копирует её в буфер обмена.

Сейчас в строку входят фиксированные TSV-колонки в таком порядке:
1. полное наименование компании;
2. ФИО руководителя;
3. ИНН;
4. телефон;
5. email;
6. номер документа;
7. дата договора в кратком формате;
8. пустая колонка под расчётный счёт;
9. комиссия;
10. минимальная комиссия;
11. три пустые колонки-заготовки для downstream-таблицы.

Если строка сформирована, в интерфейсе появляется отдельный блок с содержимым, которое можно повторно скопировать.

## Разработка и поддержка

### Что полезно знать при изменении кода

- основной orchestration и пользовательский flow сосредоточены в `MainViewModel`;
- placeholder-domain лучше менять через `PlaceholderDescriptor`, `PlaceholderBehavior` и registry, а не точечно в UI;
- если вы добавляете новое extracted поле, обычно нужно синхронно обновить:
  - built-in descriptors,
  - built-in behaviors,
  - `PromptBuilder`,
  - derived-резолверы и helper'ы `PlaceholderResolutionContext`,
  - форму подтверждения и README;
- пользовательские плейсхолдеры живут отдельно от built-in каталога и подмешиваются в runtime registry при загрузке.

### Минимальный smoke-test после изменений

1. загрузить `DOCX`-шаблон;
2. загрузить один файл реквизитов;
3. убедиться, что появилась форма;
4. нажать **«Применить»**;
5. проверить активацию кнопки **«Заполнить шаблон»**;
6. убедиться, что создаётся экспорт и появляется TSV-строка.

## Ограничения текущей версии

- Приложение работает только с шаблонами `DOCX`.
- OCR для сканированных PDF пока не реализован.
- Ошибки в UI частично логируются в консоль и не всегда выводятся пользователю как отдельные уведомления.
- Подстановка значений зависит от того, насколько предсказуемо оформлен шаблон Word.
- Качество распознавания зависит от качества исходного текста и ответа модели.
- Встроенные плейсхолдеры задаются через единый registry, а пользовательские можно добавлять отдельно.
- Для офисных форматов pipeline опирается на системные возможности macOS, а не на кроссплатформенный стек.

## Troubleshooting

### Кнопка заполнения неактивна

Проверьте, что:
- выбран шаблон `DOCX`;
- выбран файл с реквизитами;
- сохранён OpenAI API key;
- в форме нажата кнопка **«Применить»**;
- в форме нет блокирующих ошибок валидации.

### После загрузки файла реквизитов ничего не появилось

Возможные причины:
- файл содержит слишком мало читаемого текста;
- PDF состоит из сканов без текстового слоя;
- внешний extractor не смог обработать office-файл;
- OpenAI-запрос завершился ошибкой;
- ошибка выведена только в консоль Xcode.

### Плейсхолдеры не заменились

Проверьте, что:
- шаблон действительно в формате `DOCX`;
- плейсхолдер записан как `<!key!>`;
- ключ существует в словаре значений;
- вы используете правильное имя, например `company_name`, `inn`, `date`, `full_company_name`.

### API key запрашивается снова

Проверьте:
- был ли ключ сохранён без ошибок;
- доступно ли сохранение в `Keychain` на этом Mac;
- нет ли ошибок в консоли при загрузке ключа.
